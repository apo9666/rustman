import RequestContent from './Content'
import RequestRoot from './Root'
import RequestTitle from './Title'
import RequestUrl from './Url'

const Request = {
  Root: RequestRoot,
  Title: RequestTitle,
  Url: RequestUrl,
  Content: RequestContent
}

export default Request
